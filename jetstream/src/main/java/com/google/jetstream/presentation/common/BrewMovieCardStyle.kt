package com.google.jetstream.presentation.common

/** Shared landscape card variants — tray shelves vs detail/library/collection grids. */
enum class BrewMovieCardStyle {
    /** Home trays — 220dp wide, 9dp radius. */
    Tray,
    /** Detail rails, library, collection — 280dp wide, 10dp radius, subtle border. */
    Detail,
}
